name: Playwright Test

on:
  workflow_dispatch:
  push:
      branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # - name: Set Git User
      #   run: |
      #     git config --global user.email "serge.karpov@gmail.com"
      #     git config --global user.name "Serge K"

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install Dependencies
        run: npm ci

      - name: Install Playwright and Dependencies
        run: npx playwright install --with-deps chromium firefox



      # - name: Install Faker
      #   run: npm install @faker-js/faker --save-dev

      # - name: Generate Timestamp for Unique Folder Name
      #   id: timestamp
      #   run: echo "TIMESTAMP=$(date -u +"%Y%m%d%H%M%S")" >> $GITHUB_ENV
        
      - name: Run Tests
        run: npx playwright test
        continue-on-error: true

      # - name: Create and Switch to allure-report Branch
      #   run: git checkout -b allure-report
      
      
      - name: Generate Allure Report
        run: npx allure generate --clean -o allure-report
      # npx allure generate --clean -o allure-report-${{env.TIMESTAMP}}

      # mv allure-report-$TIMESTAMP $GITHUB_WORKSPACE/allure-report

      # - run: git push --set-upstream origin allure-report
      

      # - name: Commit and Push Allure Report
      #   run: |
      #     git add -A
      #     git commit -m "Update Allure report - $TIMESTAMP"
      #     git pull origin allure-report --allow-unrelated-histories
      #     git push origin allure-report
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Pushes to another repository
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.MY_SECRET_CAT}}
        with:
          source-directory: 'allure-report'
          destination-github-username: 'SHIRO-SAN-UA'
          destination-repository-name: 'PlaywrightIntro'
          user-email: serge.karpov@gmail.com
          target-branch: allure-report